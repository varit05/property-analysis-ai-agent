[project]
name = "server"
version = "1.0.0"
description = "FastAPI backend for property analysis, featuring AI-powered analysis via DeepAgent with a human-in-the-loop review workflow."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.136.3",
    "httpx>=0.28.1",
    
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.1",
    "pytest>=9.0.3",
    "uvicorn>=0.48.0",
]
